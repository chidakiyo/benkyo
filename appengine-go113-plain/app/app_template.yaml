service: ${SERVICE}
runtime: go115

handlers:
  - url: /.*
    script: auto

#vpc_access_connector:
#  name: "projects/PROJECT_ID/locations/REGION/connectors/CONNECTOR_NAME" # 置き換える

env_variables:
  MESSAGE: "${MESSAGE}"
